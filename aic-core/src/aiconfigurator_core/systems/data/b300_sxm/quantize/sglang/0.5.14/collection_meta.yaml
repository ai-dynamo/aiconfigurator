# SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
# SPDX-License-Identifier: Apache-2.0

schema_version: 1
provenance: legacy
runtime:
  framework: sglang
  version: 0.5.14
tables:
  computescale_perf:
    # 2026-08-17: device-truth replacement: the stored 0.5.14 table carried
    # device='NVIDIA GB300' (borrowed from the GB300 collection); re-collected
    # on 8x 'NVIDIA B300 SXM6 AC' (aws-pdx, manual JET runs 62938134,
    # mirrored lmsysorg/sglang:v0.5.14-cu130 amd64 image, PR #1519 branch
    # collector @ eda12dc). Values-only vs borrowed at matched keys.
    # NOTE this table is 1627/1628 zeros on every system (sub-resolution
    # kernels); the single nonzero key (m32768/k32/fp8) drops 380us -> 22us —
    # the borrowed value looks like a captured first-call hiccup.
    status: complete
  scale_matrix_perf:
    # 2026-08-17: same replacement run (median 1.027), key-identical grid,
    # zero failures.
    status: complete
