# SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
# SPDX-License-Identifier: Apache-2.0

schema_version: 1
provenance: legacy
runtime:
  framework: sglang
  version: 0.5.14
tables:
  context_attention_perf:
    # 2026-08-17: device-truth replacement: the stored 0.5.14 table carried
    # device='NVIDIA GB300' (borrowed from the GB300 collection); re-collected
    # on 8x 'NVIDIA B300 SXM6 AC' (aws-pdx, manual JET runs 63034673+63061173,
    # mirrored lmsysorg/sglang:v0.5.14-cu130 amd64 image, PR #1519 branch
    # collector @ eda12dc). Values-only vs borrowed at matched keys (median
    # 1.029); grid is a strict superset (+1906 trtllm_mha keys from case-plan
    # growth since the borrowed collection). ONE key intentionally absent:
    # trtllm_mha b32/isl1/h1/kv1/hd256/win1024 fails deterministically on the
    # amd64 build across two independent runs (passes on GB300 arm64);
    # interpolation covers the interior point.
    status: complete
  generation_attention_perf:
    # 2026-08-17: same replacement run as context (median 1.000); strict
    # superset (+2745 trtllm_mha keys), no missing keys, no duplicate keys.
    status: complete
