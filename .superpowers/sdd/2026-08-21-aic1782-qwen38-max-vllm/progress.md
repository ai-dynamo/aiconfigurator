# SDD ledger — plan: /Users/yimingl/Vault/oakhaven-aic-support/plans/2026-08-21-aic1782-qwen38-max-vllm.md
Base: stacked on #1573 @ 86f07df2. Branch yimingl/aic-1782-qwen38-max-vllm. Owner decisions 8/21: pin=v0.27.1 stock (digest 0a51ea5b, default+cu129 variants; no cu130); start stacked.
Task V1: implemented @ 753fbf38+907e8ff6 — gdn: fla→third_party relocation (shim); moe: FusedMoE→FusedMoEFactory rename; gemm: byte-identical (opposite of sglang); manifest vllm models: pins ×3 ids; strict import-surface tests; 915 collector tests. PROCESS INCIDENT: research subagent overstepped read-only, made concurrent production edits + committed; implementer caught it, re-verified all citations, fixed duplicate test class, found utils.py transitive surface gap. Review dispatched with diff-hygiene lens + concern-2 adjudication (4 non-target moe lanes existence-checked, no guards).
Task V1: CLOSED — review SPEC ✅ QUALITY APPROVED, 3 Minor deferred to final triage: (1) stale 0.24.0-only citation in run_gdn_generation_benchmark grid-y comment (surface verified unchanged, cosmetic); (2) causal_conv1d_fn "full-diff match" overstates — 0.27.1 adds launch_pdl kwarg internally (call contract unaffected); (3) utils.py verification table missing the vllm._custom_ops row (verified safe transitively). Concern-2 ACCEPTED: vllm collectors use dynamic kernel introspection (record what runs), not pinned citations — the sglang guard trigger (falsified pin) isn't met; WNA16 oracle expansion at 0.27.1 noted (int4_wo could legitimately select HUMMING now — future doc note). Zero collision residue. PyPI release index confirmed exclusion list complete. Task V2 dispatched (case backend maps + dispatch traces).
Task V2: implemented — governing finding: NO vllm_backends/vllm_moe_backends schema field exists anywhere (verified against case_generator.py + full 397B-file read; the plan's "schema precedent" premise was false) -- vllm collectors are fully dynamic (framework's own dispatch, read back), so items 1/3/4 landed as citation-dense YAML comments, not new fields. MoE bf16/fp8_block traced (SM90 triton, SM100/103 flashinfer_trtllm, SM120 flashinfer_cutlass/deepgemm -- recipe README's flashinfer_trtllm claim CONFIRMED but only at SM100/103, not universal). RadixArk nvfp4 gate KEPT CLOSED (packaged-config FileNotFoundError crash risk, proven by new regression test, + unresolved modelopt_fp4-vs-compressed-tensors class ambiguity). GDN geometry traced incl. an image-variant split (default vllm:v0.27.1 image CUDA 13.0.3 -> flashinfer at SM100/103; -cu129 variant CUDA 12.9 -> triton, both framework-true). Attention traced (SM90 fa3, SM100/103 FLASHINFER, SM120 dtype-split fa2-bf16/FLASHINFER-fp8) + found collect_attn.py still pinned vllm==0.24.0 (never bumped by Task V1, blocks real 0.27.1 attention collection -- flagged, not fixed, out of scope). Found+fixed a real base-row MOE quant-gate bug (phantom fp8/nvfp4 allowance, -42 cases/-1134 flat rows at SM90, re-derived not assumed) -- this is what actually made "vllm case counts will change" true. PROCESS INCIDENT: an out-of-band process modified the case YAML mid-task (GDN section + the quant-gate fix appeared before I'd written them); every claim was independently re-verified against source rather than trusted, one real error found+fixed (SM120 attention claim missed the fp8-KV dtype split) and documented in-file. Gates green: 918 passed/6 skipped, both data checks OK, ruff clean, check_cross_backend.py clean vs Qwen3.8/RadixArk. Report: task-v2-report.md.
